/**
 * Shared application constants.
 *
 * TEACHER_EMAIL is the single source of truth for identifying the teacher
 * account. Any component or page that needs to gate teacher-only UI or
 * routes should import this constant rather than hardcoding the email.
 */
export const TEACHER_EMAIL = "sksachin12356@gmail.com";
